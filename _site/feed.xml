<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2025-08-10T05:27:05+08:00</updated><id>/feed.xml</id><title type="html">K-r-o-s 笔记</title><subtitle></subtitle><entry><title type="html">使用 .gitignore 创建白名单</title><link href="/2025/08/10/create-whitelist-with-gitignore.html" rel="alternate" type="text/html" title="使用 .gitignore 创建白名单" /><published>2025-08-10T04:20:00+08:00</published><updated>2025-08-10T04:20:00+08:00</updated><id>/2025/08/10/create-whitelist-with-gitignore</id><content type="html" xml:base="/2025/08/10/create-whitelist-with-gitignore.html"><![CDATA[<h1 id="使用-gitignore-创建白名单">使用 .gitignore 创建白名单</h1>

<p>最近看到了这篇文章 <a href="https://rgbcu.be/blog/gitignore/"><code class="language-plaintext highlighter-rouge">.gitignore</code> is inherently Sisyphean</a></p>

<p>作者讲述了为什么需要一个白名单模式, 因为在多人维护的情况下, 会有源源不断的类似 <code class="language-plaintext highlighter-rouge">.DS_Store</code>, <code class="language-plaintext highlighter-rouge">.vscode</code>, <code class="language-plaintext highlighter-rouge">.idea</code> 之类的玩意加入到项目的 <code class="language-plaintext highlighter-rouge">.gitignore</code> 文件中, 导致其非常臃肿</p>

<p>倒不如改为白名单模式, 明确哪些是项目的必要文件</p>

<h2 id="原理和方法">原理和方法</h2>

<p>以下来自 Gemini 的回答, 我感觉足够清晰了</p>

<hr />

<p>如果你想把 .gitignore 变成白名单模式，就意味着你想要<strong>默认忽略所有文件，只指定需要追踪的文件</strong>。这通常用于项目目录中有很多临时文件、构建产物或第三方依赖，而你只关心几个特定的源代码文件。</p>

<hr />

<h3 id="实现白名单模式的步骤">实现白名单模式的步骤</h3>

<p>要实现白名单模式，你需要遵循以下两个核心步骤：</p>

<h4 id="1-忽略所有文件">1. 忽略所有文件</h4>

<p>首先，在你的 <code class="language-plaintext highlighter-rouge">.gitignore</code> 文件的开头添加一行，用通配符 <code class="language-plaintext highlighter-rouge">*</code> 忽略所有文件和文件夹：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>*
</code></pre></div></div>

<p>这行代码告诉 Git 忽略当前目录及其所有子目录中的所有文件。</p>

<h4 id="2-排除即取消忽略你需要的文件">2. 排除（即“取消忽略”）你需要的文件</h4>

<p>接下来，用 <strong><code class="language-plaintext highlighter-rouge">!</code></strong> 符号来排除（或“取消忽略”）你想要 Git 追踪的特定文件或目录。<strong><code class="language-plaintext highlighter-rouge">!</code></strong> 符号的作用是反转匹配规则，将前面被忽略的文件或目录重新包含进来。</p>

<p>例如，如果你的项目结构是这样的：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/
├── .gitignore
├── src/
│   ├── main.js
│   └── utils.js
├── README.md
├── build/
│   └── bundle.js
└── node_modules/
    └── ...
</code></pre></div></div>

<p>而你只想追踪 <code class="language-plaintext highlighter-rouge">src</code> 目录下的所有 <code class="language-plaintext highlighter-rouge">.js</code> 文件和根目录下的 <code class="language-plaintext highlighter-rouge">README.md</code> 文件，你的 <code class="language-plaintext highlighter-rouge">.gitignore</code> 文件应该这样写：</p>

<pre><code class="language-gitignore"># 忽略所有文件和文件夹
*

# 排除 .gitignore 文件本身
!/.gitignore

# 排除 README.md 文件
!/README.md

# 排除 src 目录
!/src/

# 排除 src 目录下的所有 .js 文件
!/src/*.js

# 如果 src 目录下还有子目录，且你想追踪它们，也需要排除
# !/src/**/

</code></pre>

<p><strong>关于排除目录的注意事项：</strong></p>

<p>当你排除一个目录（如 <code class="language-plaintext highlighter-rouge">!/src/</code>）时，Git 仅仅会开始追踪这个目录本身，而不会自动追踪它里面的所有文件和子目录。你还需要单独指定要追踪的子目录或文件。</p>

<hr />

<h3 id="完整的白名单模式模板">完整的白名单模式模板</h3>

<p>一个通用的白名单 <code class="language-plaintext highlighter-rouge">.gitignore</code> 文件模板可能如下所示：</p>

<pre><code class="language-gitignore"># 默认忽略所有文件和目录
*

# 排除 Git 需要追踪的关键文件和文件夹

# 1. 排除 .gitignore 文件本身
!/.gitignore

# 2. 排除重要的顶层文件，例如 README.md, LICENSE 等
!/README.md
!/LICENSE

# 3. 排除源代码目录（这个会排除整个 src 目录和其所有子目录）
!/src/

# 4. 如果你想更精确地控制，可以只排除特定文件类型
# 例如：
# !/src/*.js
# !/src/*.ts
# !/src/*.html

# 5. 如果你需要排除特定的配置文件，例如：
# !/config/
# !/config/settings.json

# 6. 如果你想排除项目中的图片等资源文件：
# !/assets/
</code></pre>

<p>使用这种方法，你的 <code class="language-plaintext highlighter-rouge">.gitignore</code> 文件可以清晰地列出项目中的“必选”文件，大大简化了大型或复杂项目的文件管理，确保你不会意外提交不应该在版本控制中的文件。</p>]]></content><author><name></name></author><category term=".gitignore" /><summary type="html"><![CDATA[使用 .gitignore 创建白名单]]></summary></entry></feed>